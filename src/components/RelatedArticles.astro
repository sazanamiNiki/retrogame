---
interface ArticleLink {
  href: string;
  icon: string;
  iconColor?: string;
  title: string;
  desc: string;
}

interface Props {
  articles: ArticleLink[];
}

const { articles } = Astro.props;
const base = import.meta.env.BASE_URL;
---

<div>
  <h3 class="text-base sm:text-lg font-bold mb-3 sm:mb-4 text-gray-800">次に読むべきおすすめ記事</h3>
  <div class="grid grid-cols-1 gap-3 sm:gap-4 sm:grid-cols-2">
    {articles.map(article => (
      <a
        href={`${base}${article.href.replace(/^\//, '')}`}
        class="block p-3 sm:p-4 bg-white rounded-lg border border-gray-200 hover:border-blue-300 hover:shadow-sm transition-[border-color,box-shadow] touch-manipulation"
      >
        <div class="flex items-center gap-2 mb-2">
          <span class={`material-symbols-outlined ${article.iconColor ?? 'text-blue-600'} text-lg`} aria-hidden="true">
            {article.icon}
          </span>
          <span class="font-bold text-gray-800 text-sm">{article.title}</span>
        </div>
        <p class="text-gray-600 text-xs">{article.desc}</p>
      </a>
    ))}
  </div>
</div>
